# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PicMap' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PicMap
  pod "Parse"
  pod "AlamofireImage"
  pod "AFNetworking"

  target 'PicMapTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PicMapUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
